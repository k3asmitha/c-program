#include<stdio.h>
int main()
{
float length,breadth,area;
printf("enter the length and breadth \n:");
scanf("%f%f",&length,&breadth);
area=length*breadth;
printf("area_of rectangle is :%f\n",area);
}
